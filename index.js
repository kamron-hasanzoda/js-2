// Math
// GitHUB

let a = "20.9999"
let b = "21"


console.log(
    // Math.floor(a) // окргуляет вниз
    // Math.ceil(a) // окргуляет вверх
    // Math.round(a) // окргуляет логически

    // Math.max(a, b, 12, 55, -123)
    // Math.min(a, b, 12, 55, -123)
    // Math.floor(Math.random() * 13)
    // Math.pow(7, 2)

    // parseInt("20.9999")
    // parseFloat(a)
);